import React from "react";

const Error = (props) => {
    console.log("error called");
    return(
        <div>
            <h3>Error: This page doesn't exist!</h3>
        </div>
    );
};

export default Error;
