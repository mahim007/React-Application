import React from "react";

const User = (props) => {
    console.log("props at user: ",props);
    
    return(
        <div>
            <h1>This is User</h1>
        </div>
    );
};

export default User;