import React from "react";

const Admin = (props) => {
    console.log("props at admin: ",props);
    
    return(
        <div>
            <h1>This is admin</h1>
        </div>
    );
};

export default Admin;